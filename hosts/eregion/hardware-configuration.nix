# PLACEHOLDER — replace this file by running the following on the target machine
# after booting a NixOS installer:
#
#   nixos-generate-config --root /mnt
#
# Copy the generated /mnt/etc/nixos/hardware-configuration.nix here.
# This stub lets the flake evaluate, but eregion cannot be deployed until
# it contains real hardware data.
{ ... }:
{
}
